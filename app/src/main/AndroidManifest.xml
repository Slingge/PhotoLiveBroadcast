<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.photolivebroadcast">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--获取验证码-->
        <activity
            android:name=".ui.establish.activity.SginInActivity"
            android:screenOrientation="portrait" />
        <!--获取验证码登录-->
        <activity
            android:name=".ui.establish.activity.VerificationActivity"
            android:screenOrientation="portrait" />
        <!--绑定手机号-->
        <activity
            android:name=".ui.establish.activity.BindPhoneActivity"
            android:screenOrientation="portrait" />
        <!--忘记密码-->
        <activity
            android:name=".ui.establish.activity.ForgetPassActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>